server:
  port: 8082

spring:
  application:
    name: clinic-core-service
  datasource:
    url: jdbc:postgresql://localhost:5432/lumera_db
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest